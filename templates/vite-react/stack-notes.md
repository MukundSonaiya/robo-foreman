# Vite + React notes

- Entry is typically `index.html` + `src/main.tsx`.
- Prefer functional components and existing state libraries already in package.json.
- Keep Vite config changes minimal; ask before adding plugins.
