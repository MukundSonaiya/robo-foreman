---
name: build
description: Apply Robo Foreman fixes non-destructively — rules, hooks, MCP merge, AGENTS.md, with backup and confirm. Supports undo from latest backup.
---

# /build — install & repair

Run the **build** skill. Present the multi-select plan from the latest scan, confirm, backup, then write.
